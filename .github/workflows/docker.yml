name: Mirror Plane Image

on:
  workflow_dispatch:

jobs:

  mirror:

    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:

    - name: Login GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}


    - name: Pull Plane image
      run: |
        docker pull makeplane/plane-server:latest


    - name: Tag image
      run: |
        docker tag \
        makeplane/plane-server:latest \
        ghcr.io/${{ github.repository_owner }}/plane-server:latest


    - name: Push image
      run: |
        docker push \
        ghcr.io/${{ github.repository_owner }}/plane-server:latest
